# 开发指南

## Phase 0 已完成 ✅

### 完成内容

1. ✅ 项目文件夹结构
2. ✅ 前端项目初始化（Vue3 + TypeScript）
3. ✅ 后端项目初始化（Node.js + Express）
4. ✅ 数据库初始化SQL脚本
5. ✅ 基础配置文件

### 项目结构

```
CareerPlanningSimulation/
├── frontend/                    # 前端项目
│   ├── src/
│   │   ├── views/              # 页面
│   │   │   ├── HomePage.vue           ✅ 首页
│   │   │   ├── LoginPage.vue          ✅ 登录/注册页
│   │   │   ├── InitialSetup.vue       ✅ 初始数值配置页
│   │   │   ├── CampusMap.vue          📝 校园地图（占位）
│   │   │   ├── ScriptDetail.vue       📝 剧本详情（占位）
│   │   │   ├── SettlementPage.vue     📝 结算页（占位）
│   │   │   └── WorkshopEditor.vue     📝 UGC编辑器（占位）
│   │   ├── components/         # 组件
│   │   ├── stores/             # 状态管理
│   │   │   ├── userStore.ts           ✅ 用户状态
│   │   │   ├── gameStore.ts           ✅ 游戏状态
│   │   │   └── scriptStore.ts         ✅ 剧本状态
│   │   ├── services/           # API服务
│   │   │   └── api.ts                 ✅ 请求封装
│   │   ├── router/             # 路由
│   │   │   └── index.ts               ✅ 路由配置
│   │   └── types/              # 类型定义
│   │       └── index.ts               ✅ 核心类型
│   ├── package.json                   ✅
│   ├── vite.config.ts                 ✅
│   └── tsconfig.json                  ✅
│
├── backend/                     # 后端项目
│   ├── src/
│   │   ├── config/
│   │   │   └── database.ts            ✅ 数据库配置
│   │   ├── middleware/
│   │   │   └── auth.ts                ✅ 认证中间件
│   │   ├── routes/
│   │   │   ├── auth.ts                ✅ 认证路由（占位）
│   │   │   ├── saves.ts               ✅ 存档路由（占位）
│   │   │   ├── scripts.ts             ✅ 剧本路由（占位）
│   │   │   └── badges.ts              ✅ 勋章路由（占位）
│   │   └── index.ts                   ✅ 入口文件
│   ├── package.json                   ✅
│   ├── tsconfig.json                  ✅
│   └── .env.example                   ✅
│
└── database/                    # 数据库
    └── init.sql                       ✅ 初始化脚本（含测试数据）
```

---

## 下一步：安装依赖并测试

### 1. 安装根目录依赖（用于并发运行）

```bash
npm install
```

### 2. 安装前后端依赖

```bash
# 方式1：使用快捷脚本
npm run install:all

# 方式2：分别安装
cd backend && npm install
cd ../frontend && npm install
```

### 3. 配置数据库

```bash
# 登录MySQL
mysql -u root -p

# 执行初始化脚本
source database/init.sql

# 或者直接执行
mysql -u root -p < database/init.sql
```

### 4. 配置后端环境变量

编辑 `backend/.env` 文件：

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=career_planning
JWT_SECRET=your_random_secret_key_here
PORT=5000
NODE_ENV=development
```

### 5. 启动项目测试

```bash
# 在项目根目录，同时启动前后端
npm run dev

# 或者分别启动
# 终端1：启动后端
cd backend && npm run dev

# 终端2：启动前端
cd frontend && npm run dev
```

### 6. 验证

- 后端健康检查：访问 http://localhost:5000/api/health
- 前端页面：访问 http://localhost:3000

---

## Phase 1：核心闭环（第1周）

### 目标
实现"登录 → 配置初始数值 → 触发剧本 → 选择选项 → 数值变化"的最小闭环

### Day 2-3：用户系统 + 存档系统

#### 后端任务
- [ ] 创建User模型（Sequelize）
- [ ] 创建UserSave模型
- [ ] 实现注册接口（密码bcrypt加密）
- [ ] 实现登录接口（生成JWT）
- [ ] 实现存档CRUD接口

#### 前端任务
- [ ] 完善LoginPage.vue的API对接
- [ ] 完善InitialSetup.vue的存档创建
- [ ] 测试登录流程

#### 验证清单
- [ ] 用户可以注册账号
- [ ] 用户可以登录并获得token
- [ ] 用户可以创建存档
- [ ] 存档数据正确保存到数据库

---

### Day 4-5：剧本系统基础

#### 后端任务
- [ ] 创建Script、ScriptNode、ScriptOption模型
- [ ] 实现剧本查询接口（支持筛选）
- [ ] 实现剧本详情接口（包含节点和选项）
- [ ] 实现存档数值更新接口

#### 前端任务
- [ ] 实现ScriptDetail.vue
  - [ ] 显示剧本内容
  - [ ] 显示选项列表
  - [ ] 点击选项触发数值变化
  - [ ] 显示反馈Toast
- [ ] 创建FeedbackToast组件
- [ ] 对接剧本API

#### 验证清单
- [ ] 可以查看剧本详情
- [ ] 点击选项后数值正确更新
- [ ] 显示反馈提示
- [ ] 完成剧本后返回地图

---

### Day 6-7：校园地图 + 事件触发

#### 后端任务
- [ ] 实现事件解锁检查逻辑
- [ ] 实现剩余事件数管理

#### 前端任务
- [ ] 实现CampusMap.vue
  - [ ] 临时背景图（纯色）
  - [ ] TopMenu组件（显示数值）
  - [ ] EventButton组件
  - [ ] 点击按钮触发剧本
- [ ] 实现ValueBar组件（五维数值显示）
- [ ] 事件触发逻辑

#### 验证清单
- [ ] 地图上显示可触发事件
- [ ] 点击事件进入剧本
- [ ] 完成剧本返回地图
- [ ] 剩余事件数正确减少
- [ ] 事件数为0时触发提示

---

## 开发注意事项

### 代码规范
- 使用TypeScript严格模式
- 遵循ESLint规则
- 组件命名使用PascalCase
- 函数命名使用camelCase

### Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```

### 调试技巧
- 前端：使用Vue DevTools
- 后端：使用console.log或VS Code调试
- 数据库：使用MySQL Workbench或命令行

### 常见问题

1. **端口被占用**
   - 修改backend/.env的PORT
   - 修改frontend/vite.config.ts的server.port

2. **数据库连接失败**
   - 检查MySQL是否启动
   - 检查.env配置是否正确
   - 检查数据库是否已创建

3. **CORS错误**
   - 后端已配置cors中间件
   - 前端使用vite proxy

---

## 当前状态：Phase 0 完成 ✅

**可以开始 Phase 1 开发了！**

请先执行上述"安装依赖并测试"步骤，确保环境正常运行。
